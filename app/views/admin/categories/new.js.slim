| $('#modal_window').addClass('text-center')
                    .modal('show')
                    .html("#{j(render partial: 'form', locals: { title: t('.new'), url: admin_categories_path, method: 'post' })}");
