.block
  .row-fluid
    .span3
      | "#{ t('.first_name') }"
    .span3
      = @profile.first_name
  .row-fluid
    .span3
      | "#{ t('.last_name') }"
    .span3
      = @profile.last_name
  .row-fluid
    .span3
      | "#{ t('.phone_number') }"
    .span3
      = "(+#{@code}) #{@profile.phone_number}"
  .row-fluid
    .span3
      | "#{ t('.gender') }"
    .span3
      = @profile.gender
  .row-fluid
    .span3
      | "#{ t('.country') }"
    .span3
      = "#{@country}"
  .row-fluid
    =link_to t('edit'), edit_profile_path(@profile), class: 'btn btn-success'
        
